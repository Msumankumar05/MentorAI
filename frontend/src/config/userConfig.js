/**
 * Central user config — change this ONE place to update the name
 * everywhere in the app (welcome screen, sidebar, avatar, etc.)
 *
 * In a real auth system, replace this with a fetch from your backend.
 */
const userConfig = {
    name: 'M Suman Kumar',
    firstName: 'Suman',
    avatar: 'MS',
    plan: 'Pro',
    email: 'ms@example.com',
};

export default userConfig;
